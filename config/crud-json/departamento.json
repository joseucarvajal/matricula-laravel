{
    "fields": [
        {
            "name": "nombre",
            "type": "string"
        }
    ],
    "validations": [
        {
            "field": "nombre",
            "rules": "required|max:30"
        }
    ]
}